#version 330 core

layout(location = 0) in vec3 aPos; // position du sommet

uniform mat4 MVP; // matrice Model*View*Projection

void main()
{
    // transforme la position du sommet
    gl_Position = MVP * vec4(aPos, 1.0);
}
